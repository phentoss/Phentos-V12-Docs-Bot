const Discord = require("discord.js");

module.exports = async bot => {
  bot.user.setActivity(`!yardım 💛 Phentos`, { type: "WATCHING" });
  console.log(`${bot.user.username} is ready ! [This bot is Phento's helper] 💛 Phentos#4545! || `);
};
